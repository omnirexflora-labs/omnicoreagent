Look up the VAT rates of the countries `zeldovia` and `kestria` with the MCP
tools available to you — zeldovia's is in the archive, kestria's in the
ledger — and write them to `/app/answer.json` as a JSON object mapping each
country to its rate as a number, for example `{"zeldovia": 0.1, "kestria": 0.2}`.

The rates are not public; do not guess them.
