---
title: OmniCoreAgent
description: 'The open Python agent runtime and harness, with an SDK, for AI applications that have to hold up in production'
mode: 'wide'
---

<img
  className="block dark:hidden"
  src="/assets/IMG_5292.jpeg"
  alt="OmniCoreAgent Light"
/>
<img
  className="hidden dark:block"
  src="/assets/IMG_5292.jpeg"
  alt="OmniCoreAgent Dark"
/>

## The runtime around the model

A model is not an agent. **OmniCoreAgent** is everything around it: the loop,
tools, memory and files — and, once the agent can do real things, the policy
that says what it may do, the sandbox its code runs in, the record that survives
a crash, the budget that stops it spending, and the evidence of every run.

OmniCoreAgent is an agent **runtime and harness**, used through a **Python
SDK**. One agent object, from a first script to a governed background worker
on a server, and onto a benchmark.

| | What it means here |
|---|---|
| **Runtime** | What executes and keeps the work: runs and their state, the durable record, workers, sandboxes, persistence. |
| **Harness** | What surrounds the model and controls each step: the loop, context, tool execution, approvals, budgets, recovery, telemetry. |
| **SDK** | What you write against: `OmniCoreAgent`, `ToolRegistry`, the configuration, the methods, the CLI and the HTTP API. |

<Frame>
  <img src="/assets/how-a-run-works.svg" alt="How a run works: your app calls OmniCoreAgent, which governs every action with a policy, a budget and a sandbox, talks to the model and to tools, and keeps the evidence of the run." />
</Frame>

<CardGroup cols={3}>
  <Card title="It acts safely" icon="shield-check">
    A policy allows, denies or **asks a person** before each action. Commands
    run in a sandbox that is not given your keys and tokens. A budget prices
    each model call and holds that amount before the call is made.
  </Card>
  <Card title="It survives" icon="rotate">
    A run pauses for a person or a top-up and resumes where it stopped — with
    a durable memory store, even after a crash, in another process — and never
    repeats a call silently.
  </Card>
  <Card title="It leaves evidence" icon="magnifying-glass-chart">
    Every run is kept: what the model saw at each step, what it did, what came
    back, outcomes that arrive later, and records to evaluate or train on.
  </Card>
</CardGroup>

## Start in three steps

<Steps>
  <Step title="Install">
    ```bash
    pip install omnicoreagent
    ```
  </Step>
  <Step title="Set your model key">
    ```bash
    export LLM_API_KEY=your_api_key_here
    ```
  </Step>
  <Step title="Build something">
    <CardGroup cols={2}>
      <Card title="Quickstart — 5 minutes" icon="bolt" href="/docs/getting-started/quickstart">
        An agent, a tool, a memory, and the evidence of its run.
      </Card>
      <Card title="Tour — 15 minutes" icon="map" href="/docs/getting-started/tour">
        A policy that asks a person, a sandbox, a budget.
      </Card>
    </CardGroup>
  </Step>
</Steps>

## See it

```python
import asyncio
from omnicoreagent import OmniCoreAgent, ToolRegistry

tools = ToolRegistry()

@tools.register_tool("lookup_order")
def lookup_order(order_id: str) -> dict:
    """Look an order up in the application's own store."""
    return {"order_id": order_id, "status": "shipped", "carrier": "DHL"}

agent = OmniCoreAgent(
    name="support",
    system_instruction="You answer questions about orders, using the tools. Answer in plain text.",
    model_config={"provider": "openai", "model": "gpt-5.6-terra"},
    local_tools=tools,
)

async def main():
    result = await agent.run("Where is order 1042?", session_id="customer-7")
    print(result["response"])

    trajectory = await agent.get_trajectory(result["trace_id"])
    for step in trajectory["steps"]:
        for call in step["tool_calls"]:
            print(step["step"], call["tool_name"], call["arguments"], "->", call["observation"]["content"])

    await agent.cleanup()

asyncio.run(main())
```

```text
Order 1042 has shipped via DHL.
1 lookup_order {'order_id': '1042'} -> {"tool_name": "lookup_order", "args": {"order_id": "1042"}, "status": "success", "data": {"order_id": "1042", "status": "shipped", "carrier": "DHL"}, "message": null}
```

The second line is the run's **trajectory**: the step, the tool the model
chose, its arguments, and exactly what the model received back.

## Find your way

<Tabs>
  <Tab title="Build">
    <CardGroup cols={2}>
      <Card title="Tools" icon="toolbox" href="/docs/core-concepts/local-tools">
        Your Python functions as tools, in parallel batches.
      </Card>
      <Card title="MCP servers" icon="plug" href="/docs/core-concepts/mcp">
        stdio, SSE and streamable HTTP, with OAuth.
      </Card>
      <Card title="Code mode" icon="code" href="/docs/core-concepts/code-mode">
        The model writes a program that calls your tools.
      </Card>
      <Card title="Skills" icon="wand-magic-sparkles" href="/docs/core-concepts/skills">
        Packaged instructions and scripts the agent can use.
      </Card>
      <Card title="Memory" icon="database" href="/docs/core-concepts/memory">
        Sessions in memory, Redis, Postgres or MongoDB.
      </Card>
      <Card title="Workspace files" icon="folder-tree" href="/docs/core-concepts/workspace-files">
        Files for notes and artifacts, local, S3 or R2.
      </Card>
      <Card title="Context" icon="brain-circuit" href="/docs/core-concepts/context-engineering">
        Long tasks without running out of context.
      </Card>
      <Card title="Sub-agents" icon="users" href="/docs/core-concepts/sub-agents">
        Workers the lead agent spawns and reads back.
      </Card>
      <Card title="Models" icon="microchip" href="/docs/how-to-guides/models">
        OpenAI, Anthropic, Gemini, Groq, DeepSeek, Ollama and more.
      </Card>
      <Card title="Events and streaming" icon="signal-stream" href="/docs/core-concepts/events">
        Follow a run as it happens.
      </Card>
    </CardGroup>
  </Tab>
  <Tab title="Make it safe">
    <CardGroup cols={2}>
      <Card title="Security model" icon="shield-halved" href="/docs/core-concepts/security-model">
        Policies — allow, ask, deny — over every capability.
      </Card>
      <Card title="Execution and sandboxes" icon="terminal" href="/docs/core-concepts/execution">
        The execute tool, and where its commands run.
      </Card>
      <Card title="Sandbox providers" icon="box" href="/docs/how-to-guides/sandbox-providers">
        Docker, E2B, Modal, Daytona, Vercel, your own, or local.
      </Card>
      <Card title="Guardrails" icon="shield" href="/docs/core-concepts/guardrails">
        Prompt-injection screening on what the agent reads.
      </Card>
    </CardGroup>
  </Tab>
  <Tab title="Run it">
    <CardGroup cols={2}>
      <Card title="Durable runs and budgets" icon="rotate" href="/docs/core-concepts/durable-runs">
        Approvals, top-ups, checkpoints, recovery after a crash.
      </Card>
      <Card title="Background agents" icon="clock" href="/docs/core-concepts/background-agents">
        Scheduled and queued work, with retries and leases.
      </Card>
      <Card title="OmniServe" icon="server" href="/docs/how-to-guides/omniserve">
        REST and SSE for runs, approvals, budgets and traces.
      </Card>
      <Card title="In a container" icon="docker" href="/docs/how-to-guides/agent-in-a-container">
        Package and deploy an agent.
      </Card>
    </CardGroup>
  </Tab>
  <Tab title="See and improve">
    <CardGroup cols={2}>
      <Card title="Observability" icon="magnifying-glass-chart" href="/docs/how-to-guides/observability">
        Trajectories, outcomes, training records, and exporters.
      </Card>
      <Card title="Headless runs" icon="terminal" href="/docs/how-to-guides/headless-runs">
        `omnicoreagent run` for CI and scripts.
      </Card>
      <Card title="Harbor and Terminal-Bench" icon="flask" href="/docs/how-to-guides/harbor">
        Run the agent on benchmark tasks; read every trial.
      </Card>
      <Card title="Configuration" icon="gear" href="/docs/how-to-guides/configuration">
        Every setting, and the environment variables.
      </Card>
    </CardGroup>
  </Tab>
</Tabs>

## Using an AI coding agent?

<CardGroup cols={2}>
  <Card title="Use these docs with AI tools" icon="sparkles" href="/docs/getting-started/use-docs-with-ai-tools">
    Ask AI, `llms.txt`, copy as Markdown, the docs MCP server, Cursor and VS Code.
  </Card>
  <Card title="AGENTS.md" icon="map" href="https://github.com/omnirexflora-labs/omnicoreagent/blob/main/AGENTS.md">
    A map of the repository for coding agents: what lives where, and how to work in it.
  </Card>
</CardGroup>
